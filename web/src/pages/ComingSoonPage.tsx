// Placeholder for a dashboard section not built yet — each is replaced by its
// own real page as CBLT-243/244/245 land (CBLT-304 only builds the shell).
function ComingSoonPage({ title }: { title: string }) {
  return (
    <div>
      <h1 className="text-xl font-semibold text-gray-900">{title}</h1>
      <p className="mt-2 text-sm text-gray-500">This section hasn't been built yet.</p>
    </div>
  )
}

export default ComingSoonPage
